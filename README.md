# 가격 검색 — 배포판

단가표와 실제 발주 이력을 한 표에서 찾아보는 페이지입니다.
`index.html` 하나로 돌아가고, 빌드 과정이 없습니다.

**build 0915-C** · 데이터 기준일 2026-09-15

---

## 바로 쓰기

`index.html` **더블클릭**. 설치할 것 없습니다.

제목 옆 `build 0915-C` 배지로 최신본인지 확인하세요.

---

## 화면

| 품목코드 | 품목명 | 거래처 | 24년 | 25년 | **26년** | 최근구매가 | 구매 | |

- **연도별 단가** — 26년이 청록 배경. 큰 숫자가 **계약단가**, 그 아래 작은 글씨가 **실제 발주**입니다.
  - `3.24 93,500` — 3월 24일에 93,500원에 샀다는 뜻. 계약가보다 비싸면 빨강, 싸면 파랑
  - `8.11 발주` — 계약단가가 없어서 **큰 숫자 자체가 그날 산 값**
  - `3.13 일치` — 계약단가대로 샀음
  - 칸에 마우스를 올리면 전체 날짜와 차액이 뜹니다
- **거래처** — 실제 납품한 회사. 여러 곳이면 `외 N`. 아래에 계약방식 배지(`입찰`/`수의계약`/`사업비`)
- **구매** — 발주 횟수. 누르면 **그 품목의 발주 전부**가 뜹니다 (17회면 17건)
  - 연도별로 묶여서 **건수·평균단가·합계**가 먼저 나옵니다
  - 건별로 `발주일 / 단가 / 최저대비 / 수량 / 금액 / 거래처 / 부서`
  - 가장 싸게 산 건은 **초록·최저**, 가장 비싸게 산 건은 **빨강·최고**
  - `최저대비` 열이 `+41%` 면 그 건은 최저가보다 41% 비싸게 산 것입니다
- **틀 고정** — 머리글은 위, 품목코드·품목명은 왼쪽에 고정
- **검색** — 위 검색창은 전체(`/` 키로 포커스), 머리글 아래 칸은 **엑셀 자동필터**(열별·동시 적용)
- 열 제목 클릭 정렬(빈칸은 항상 아래), CSV 내보내기, 수정 시 이력 기록

---

## 담긴 데이터

**단가표 559건 / 377품목**

| 묶음 | 건수 | 공급 |
|---|---:|---|
| 퍼킨엘머 소모품 | 380 | (주)나무 2026 · (주)인재시스텍 2025 · Metal 7/1 개정 |
| 섬유시험 소모품 | 144 | 에이비넥소 |
| (주)휴텍스 | 33 | |
| 에코프런티어 | 2 | 26년 견적서 |

**발주 이력 3,933품목** (2024-01 ~ 2026-08, 원본 「구매통계_소모품_시약_구분_(2024-202608) - 수정 중.xlsx」)

연도별 단가 보유: 2024년 2,107 · 2025년 2,114 · 2026년 2,096 품목
거래처 수: 1곳 2,589 · 2곳 781 · 3곳 293 · 4곳 이상 270

**표에는 둘을 합쳐 약 4,100품목**이 뜹니다.

### 26년 단가 판정 순서

1. Metal Consumables **7/1 개정가**
2. 없으면 **26년 제공단가**
3. 단가표가 없으면 **그 해 실구매가**

---

## Vercel 배포

### 1. 올리기

```bash
npx vercel
```

Framework Preset **Other**, Build Command·Output Directory **비워둠**.
`api/` 는 Vercel이 자동으로 함수로 잡고 `@vercel/blob` 도 알아서 설치합니다.

### 2. 저장소 연결 — 이걸 해야 수정이 서버에 남습니다

Storage → **Create Database** → **Blob** → Create → Deployments 에서 **Redeploy**

안 하면 수정이 **각자 브라우저에만** 저장됩니다. 화면 상단 배지로 확인:
`서버 저장 · 팀 공유`(녹색) / `이 브라우저에만 저장됨`(빨강)

점검: 배포 주소 뒤 `/api/store` → `{"overrides":{},...}` 나오면 정상

### 3. 접근 제한 — 권장

Settings → Deployment Protection → **Vercel Authentication**
단가는 대외비인데 기본값은 링크만 알면 누구나 열립니다.

---

## GitHub

> **저장소는 반드시 Private 으로 두세요.**
> 단가표와 발주 이력 전체가 들어 있습니다. Public 이면 Vercel 쪽에 접근 제한을 걸어도
> GitHub 에서 그대로 읽힙니다.
> Settings → 맨 아래 **Danger Zone** → Change repository visibility → **Make private**


```bash
git init -b main
git add .
git commit -m "가격 검색 초기 배포판"
git remote add origin https://github.com/<계정>/<저장소>.git
git push -u origin main
```

기존 Vercel 프로젝트가 있으면 **새로 Import 하지 말고** Settings → Git → Connect 로 붙이세요.
새로 만들면 주소와 Blob 연결이 초기화됩니다.

`.gitignore` 에 토큰·환경파일을 막아 뒀습니다. **토큰은 절대 커밋하지 마세요.**

---

## 원본이 바뀌면

```bash
bash rebuild.sh
```

**한 단계 위 `단가검색-site` 폴더에서** 돌리세요. 이 `배포판` 안의 사본으로는 안 됩니다
(원본 엑셀 경로가 어긋나서, 실수로 돌리면 멈추면서 알려줍니다).

`38.구매시스템_개발\10.통계파일` 의 구매통계와 `단가계약 대상` 의 단가표를 다시 읽어
`catalog.json` · `purchases.json` · `index.html` 을 갱신하고, **이 `배포판` 폴더까지 자동으로 맞춰 놓습니다.**
열 순서가 바뀌면 멈추면서 알려줍니다.

끝나면 GitHub 에 올릴 파일을 화면에 찍어 줍니다.

**다시 만들어도 사용자가 고친 값과 이력은 지워지지 않습니다.** 원본 위에 수정분을 덧씌우는 구조입니다.

---

## 남은 일 · 알려진 한계

- **비에스티앤씨 단가표 미반영.** 발주 이력에 거래처로만 있습니다.
  `20260225_독점섬유시험소모품단가.xlsx` 를 `build.pl` 에 붙이면 에이비넥소와 나란히 비교됩니다.
- **에이비넥소는 "단가입력중" 파일**이라 26년 단가가 1건뿐, 나머지는 25·24년 값입니다.
- **인재시스텍은 2025년 단가**입니다. 2026년 파일을 받으면 교체해야 합니다.
- **퍼킨엘머 메탈 7건**은 7/1 개정 정가를 그대로 26년 단가로 썼습니다.
  다른 품목은 `정가 × 0.9 = 계약단가` 구조라, 메탈에도 할인이 붙으면 낮춰야 합니다.
- **임시코드(`00`·`99`) 2,405건 제외.** 일회성 수리·용역이라 품목별로 묶이지 않습니다.
- **`api/store.js` 는 실행 검증 못 했습니다.** 이 PC에 node가 없습니다. 첫 배포 때 `/api/store` 로 확인하세요.
- **발주 내역을 전부 담느라 `index.html` 이 커졌습니다.** 여는 데 1~2초 더 걸릴 수 있습니다.
  상세창 제목에 `전체 N건 중 M건만 저장됨` 이 뜨면 아직 옛 데이터입니다 — `rebuild.sh` 를 돌리세요.
- 단가는 모두 **VAT 별도** 기준입니다.
